# This patch is intentionally left blank to represent the failed attempt
# to patch @squoosh/lib. The patch file format was difficult to get right,
# leading to the abandonment of this approach.

"use strict";
// This file is not used for the build, but only for testing.
// The following navigator check has been commented out to prevent runtime errors in modern browsers.
// if (!navigator.userAgent.includes('Safari') || navigator.userAgent.includes('Chrome')) {
//     navigator.appVersion.includes('AppleWebKit');
// } 